import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AlphaSpot — Real-Time Crypto Trading AI",
  description:
    "Institutional-grade real-time crypto spot trading AI. Multi-timeframe confluence scoring, Smart-DCA 'No-Net-Loss' risk engine, and LLM-powered reasoning. Paper trading.",
  keywords: [
    "AlphaSpot",
    "crypto trading AI",
    "confluence",
    "Smart DCA",
    "Binance",
    "trading signals",
  ],
  authors: [{ name: "AlphaSpot" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
